[project]
name = "snow-crash"
version = "0.1.0"
description = "TUI chat client for Ollama"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ollama>=0.6.1",
    "textual>=8.2.1",
]

[project.scripts]
snow-crash = "snow_crash.app:main"
